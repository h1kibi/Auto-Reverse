{
  "name": "stdin_memcmp",
  "description": "Flag checked via stdin memcmp",
  "flag_regex": "flag\\\\{[^}]+\\\\}",
  "expected_flag": "flag{stdin_memcmp_test}",
  "input_channel": "stdin",
  "allowed_solvers": ["dynamic_trace", "angr_path"],
  "must_verify": true,
  "max_seconds": 60
}
